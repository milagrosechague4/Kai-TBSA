# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets — NEVER commit
.env
.env.*
!.env.example

# Bearer tokens (the whitelist) — secrets, NEVER commit
tokens.json

# Company brains (tenant data) — NEVER commit; lives in Drive/volume
data/

# DB (legacy local postgres dir from the v1 stack)
pgdata/

# OS
.DS_Store
connect-urls.local.md
